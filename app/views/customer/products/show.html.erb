<div class='container'>
  <div class='row mt-3'>
    <%= render 'customer/homes/genre-search' %>


    <div class="col-md-3">
      <!-- 商品画像 -->
      <%= attachment_image_tag @product, :image, format: 'jpeg', fallback: "no_image.jpg", size: "150x150" %>
    </div>
    <div class="col-md-6">
      <!-- 商品名 -->
      <%= @product.name %><br>
      <!-- 商品説明 -->
      <%= @product.introduction %><br>
      <!-- 価格 -->
      <%= @product.price.to_s(:delimited) %>円<br>
      <!-- カートに入れるボタン -->
    </div>
  </div>
</div>